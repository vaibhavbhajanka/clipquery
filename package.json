{
  "name": "clipquery",
  "version": "1.0.0",
  "description": "Video search and chat application",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
